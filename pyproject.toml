[project]
name = "3d-models"
version = "0.1.0"
description = "Parametric 3D models for FDM printing (Bambu Lab A1 Mini)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cadquery>=2.7.0",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
